- name: Deploy to kind
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Create kind cluster
      command: kind create cluster --name molecule
      failed_when: false
      changed_when: false

    - name: Deploy manifests
      command: kubectl apply -f {{ role_path }}/../../../k8s